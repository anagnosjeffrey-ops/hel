# Connection used by the server. The test suite uses TEST_DATABASE_URL, falling
# back to the same local database if it is unset.
DATABASE_URL=postgres://autobank:autobank@127.0.0.1:5432/autobank
TEST_DATABASE_URL=postgres://autobank:autobank@127.0.0.1:5432/autobank_test

HOST=0.0.0.0
PORT=3000
